import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link to={"/"} className="font-playfair text-xl font-bold theme-dark-text block">
      ATELIER
    </Link>
  );
};
export default Logo;
